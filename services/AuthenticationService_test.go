package services

import (
	"testing"
)

func TestAuthenticate(t *testing.T) {
	// authenticationRepository := new(mocks.IAuthenticationRepository)
	// var authentication models.AuthenticationModel
	// authentication.UserId = 1
	// authenticationRepository.On("GetAuthenticationByToken", "token").Return(authentication, nil)
	//
	// authenticationService := AuthenticationService{authenticationRepository}
	//
	// expectedResult := authentication
	// actualResult, _ := authenticationService.Authenticate("token")
	// assert.Equal(t, expectedResult, actualResult)
}
